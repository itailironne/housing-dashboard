# STATUS: Auto-update check failed (2026-08-10)

## מה נבדק
- נקרא `HANDOFF.md` ו-`data/last_release.json` במלואם. הנתון הנוכחי בדשבורד:
  הודעה `189/2026`, פורסמה 2026-06-18, מכסה אפריל 2025 - מרץ 2026.
  ההודעה הבאה הוכרזה ל-2026-09-17 (עדיין לא הגיעה נכון ל-10.8.2026).

## מה נכשל
בסביבת ההרצה המרוחקת (remote execution environment) של ה-routine המתוזמן הזה,
**הגישה ל-`www.cbs.gov.il` חסומה ברמת ה-egress proxy הארגוני** — לא ברמת
robots.txt/User-Agent כפי שתועד ב-HANDOFF.md לגבי הרצה מקומית.

- `curl` ישיר (גם עם User-Agent של דפדפן) מחזיר מהפרוקסי `403 Forbidden` על
  ה-CONNECT tunnel ל-`www.cbs.gov.il:443`, לפני שהבקשה בכלל יוצאת לרשת.
  `curl -sS "$HTTPS_PROXY/__agentproxy/status"` מאשר זאת תחת
  `recentRelayFailures`:
  ```
  {"kind":"connect_rejected","detail":"gateway answered 403 to CONNECT
   (policy denial or upstream failure)","host":"www.cbs.gov.il:443"}
  ```
- כלי ה-`WebFetch` נכשל גם הוא עם שגיאה מפורשת:
  `EGRESS_BLOCKED: Access to www.cbs.gov.il is blocked by the network egress proxy.`
- לפי הוראות הסביבה: חסימת 403/407 מהפרוקסי היא מדיניות ארגונית ואסור לנסות
  לעקוף אותה — יש לדווח עליה, לא לנחש/לדלג.

## מה כן נמצא (לא אומת!)
חיפוש (`WebSearch`) על "cbs.gov.il התחלות וגמר בנייה הודעה לתקשורת 2026" ו-
"...אוגוסט 2026" החזיר, בין השאר, קישור PDF שלא הופיע קודם ב-`last_release.json`:

- `https://www.cbs.gov.il/he/mediarelease/DocLib/2026/147/04_26_147b.pdf`
  (מספר הודעה 147/2026 — שונה מ-189/2026 הרשום כרגע)

**לא הצלחתי לפתוח/לאמת קובץ זה** (לא ניתן לדעת אם הוא מוקדם או מאוחר יותר
מ-189/2026, ואיזו תקופה הוא מכסה) בגלל חסימת ה-egress שתוארה למעלה. ייתכן
שזו הודעה ישנה יותר (למשל מרץ-אפריל 2026, לפי טווח המספור שנצפה בעבר סביב
089-189/2026), אבל זה ניחוש שלא מאומת מול המקור — אסור להסתמך עליו.

שאילתות חיפוש נוספות שנוסו:
- "cbs.gov.il התחלות וגמר בנייה הודעה לתקשורת 2026"
- "cbs.gov.il \"התחלות וגמר בנייה\" הודעה לתקשורת אוגוסט 2026"

## מה לא נעשה (בכוונה)
לא נגעתי ב-`housing_construction_dashboard.html` ולא ב-`data/last_release.json` —
אין מקור מאומת לעדכן מולו.

## המלצה לבעל הדשבורד
- זו כנראה בעיית תשתית של סביבת ה-routine (מדיניות egress חוסמת את כל
  cbs.gov.il), לא בעיה במתודולוגיה שתועדה ב-HANDOFF. כדאי לבדוק/לעדכן את
  מדיניות ה-egress של הסביבה כדי לאפשר גישה ל-`www.cbs.gov.il`, או להריץ את
  ה-routine בסביבה שבה הגישה פתוחה (כפי שתועד שעבד בהרצה מקומית קודמת).
- אם הבעיה חוזרת גם בהרצות הבאות, שווה לבדוק אם ה-routine כרגע מוגדר לרוץ
  בסביבה מרוחקת שונה מזו שתועדה כעובדת ב-HANDOFF.
