# דשבורד דירות לפי שלב בנייה — מסמך העברה ל-Claude Code

## מה זה
דשבורד HTML אינטראקטיבי (`housing_construction_dashboard.html`) שמציג נתוני הלמ"ס על
דירות לפי שלב בנייה: בבנייה פעילה, גמר בנייה, התחלת בנייה, היתרי בנייה.
קובץ עצמאי לחלוטין — Chart.js מוטמע בפנים, אין תלות ברשת חיצונית.

## מקור הנתונים
טבלה 1 מהלמ"ס (`04_26_189t1.xls`), נתונים רבעוניים 2018 Q1 – 2026 Q1.
הנתונים מוטמעים בקובץ עצמו בתוך `const RAW_DATA = {...}` (בתחילת ה-`<script>` הראשי).
מבנה RAW_DATA:
- `labels`: מערך תוויות רבעונים ("2018 Q1", ...)
- `active_construction` + `active_pct`: בבנייה פעילה (אין לה trend/sa, רק original)
- `completed` / `begun` / `permits`: כל אחד עם `{original, trend, sa, pct}`

בנוסף, `const CITY_DATA = {...}` (מיד אחרי RAW_DATA) — פילוח לפי יישוב, ראו פרק
"הושלם: נתוני התחלות בנייה לפי יישוב" למטה למקור ולמבנה המלא.

## מבנה הקוד (בתוך ה-HTML)
- `CATEGORIES`: מערך של 4 קטגוריות עם `key/title/subtitle/color`, ול-`begun` יש גם
  `showSAOverlay: true`
- `getSeriesData(cat)`: מחזיר את המערך המלא (original/trend/sa) לפי הבחירה בתפריט
- `sliceData(arr, start, end)`: חיתוך טווח (כולל end, inclusive)
- `computeYoYGrowth(fullSeries)`: צמיחה שנה מול שנה (4 רבעונים אחורה), **מחושב על
  המערך המלא ולא על החלק החתוך**, כדי שנקודות בתחילת טווח מצומצם עדיין יראו נתון
- `buildCharts()`: בונה מחדש את כל 4 הכרטיסים/הגרפים (הורס ובונה instances, לא מעדכן)
- `updateCharts()`: קורא ל-`buildCharts()` מחדש (rebuild מלא, לא patch)
- `buildTable()`, `buildSummary()`: הטבלה המלאה וכרטיסי הסיכום למעלה
- `buildCitySummary()`, `buildCityChart()`, `buildCityTable()`,
  `sortedCitiesForTable()`, `citiesByLatestValue()`: טאב "לפי יישוב" (ראו פרק
  ייעודי למטה)
- מעבר טאבים: לולאה גנרית על `.tab-panel` לפי `panel.id === 'tab-' + target`
  (לא hardcoded למספר טאבים ספציפי — תומך הוספת טאבים נוספים בעתיד בלי שינוי
  בלוגיקת המעבר עצמה)

## פיצ'רים שנבנו (בסדר כרונולוגי)
1. דשבורד בסיסי: 4 גרפים + טבלה + כרטיסי סיכום, RTL עברית, עיצוב כהה
2. תיקון קריטי: Chart.js **מוטמע בקובץ** (לא CDN) + עטוף ב-IIFE שמסתיר
   `define/module/exports` — כדי למנוע התנגשות UMD עם AMD loaders בסביבת התצוגה
   (זו הייתה הסיבה ל-"Chart is not defined" שקרה גם אחרי הטמעה ראשונית)
3. בורר טווח זמן: כפתורים קבועים (8 רבעונים / 5 שנים / הכל) + בורר מותאם אישית
   (`customFrom`/`customTo` selects)
4. קו צמיחה על ציר Y משני (`yAxisID: 'y1'`) — עבר כמה איטרציות:
   - קודם: רגרסיה לינארית על כל הטווח (בוטל)
   - אז: רבעון מול רבעון (בוטל)
   - **גרסה נוכחית: שנה מול שנה** (רבעון X מול אותו רבעון בשנה הקודמת)
5. קו "מנוכה עונתיות" (SA) — overlay נוסף על גרף "התחלת בנייה" בלבד (`showSAOverlay`)
6. טאב נפרד "🔄 איך לעדכן נתונים" עם הוראות איך לרענן את הדשבורד כשיוצא קובץ חדש
7. טאב שלישי "🏙️ לפי יישוב" — פילוח התחלות בנייה לפי 33 יישובים (סה"כ מעל 500
   דירות ב-12 החודשים האחרונים), נתון שנתי מתגלגל מ"לוח א'" בהודעת הלמ"ס
   (ולא מקובץ ה-xls של טבלה 1). כרטיסי סיכום, גרף עמודות אופקי עם בורר Top-N,
   וטבלה מלאה עם מיון בלחיצה על כותרת

## איך לבדוק שהכל עובד (בלי דפדפן אמיתי)
נבנתה שיטת בדיקה עם jsdom + node-canvas (ראה היסטוריית השיחה) כי:
- Chart.js לא ניתן לבדיקה ישירה תחת jsdom+node-canvas עקב חוסר תאימות (`getAttribute`,
  `setTransform` וכו') — זו מגבלת סביבת בדיקה, לא באג אמיתי
- הפתרון: הסרת ספריית Chart.js האמיתית מה-HTML לבדיקה, והזרקת מחלקת `Chart` stub
  פשוטה דרך `beforeParse` של jsdom, כדי לבדוק את **הלוגיקה שלנו** (datasets, טווחים,
  toggles) בלי להריץ רינדור קנבס אמיתי
- כל שינוי קוד לוּוה ב: `node --check` לבדיקת תחביר + סקריפט jsdom ממוקד שמוודא
  שהערכים המחושבים תואמים בדיוק לנתוני המקור (בדקנו את זה גם ידנית מול העמודות
  המקוריות בקובץ ה-xls)

## הושלם: נתוני התחלות בנייה לפי יישוב (2026-08-10)
נוסף טאב שלישי "🏙️ לפי יישוב" עם פילוח עירוני, בהמשך למשימה שהייתה פתוחה כאן.

**גישה ל-PDF**: בניגוד למה שנכתב למעלה, `curl` ישיר (עם User-Agent של דפדפן) דרך
Bash **כן הצליח** להוריד את ה-PDF מ-cbs.gov.il (200 OK, ~1MB) — למרות שה-WebFetch
tool/גלישה בצ'אט חסומים. כנראה שה-robots.txt/חסימה חלים ברמת ה-tool הספציפי ולא
ברמת הרשת של הטרמינל. אם זה חוזר לקרות בעתיד: **תמיד לנסות `curl` ישיר קודם**
לפני לבקש מהמשתמש להעלות קובץ ידנית.

**חילוץ הטבלה**: `pdftotext -layout` נתן טקסט עם שמות יישובים משובשים/חסרים
(בעיית קידוד RTL) וסדר עמודות לא אמין. הפתרון: Python (`pdfplumber`,
`page.extract_tables()`) עם ה-python.exe האמיתי (לא ה-stub של
`AppData\Local\Microsoft\WindowsApps\python.exe` שמפנה ל-Microsoft Store) —
`C:\Users\Ayelet Lironne\AppData\Local\Programs\Python\Python312\python.exe`.
ה-extraction נתן שמות יישובים הפוכים תו-אחר-תו (למשל `'יצרא לכה ךס'` במקום
`'סך הכל ארצי'`) — תופעה נורמלית של pdfplumber עם RTL, פשוט להפוך את המחרוזת.
המספרים אומתו בשתי דרכים: (1) חישוב מחדש של אחוז השינוי מתוך שני הערכים
האחרונים והשוואה לעמודת "אחוז שינוי" שבטבלה, (2) השוואה לטקסט הנרטיבי בעמוד 6
של ההודעה (שמזכיר כמה ערים ספציפיות ואחוזים). שתי הבדיקות עברו ב-100%.

**המבנה שנוסף** (`CITY_DATA`, ב-`<script>` הראשי, אחרי `RAW_DATA`): 33 יישובים
(כל אלה עם מעל 500 דירות ב-12 החודשים אפריל 2025–מרץ 2026) + שורת `total`,
כל אחד עם `values` (4 תקופות שנתיות מתגלגלות, 2022-23 עד 2025-26) ו-`pctChange`.
`values`/`pctChange` הם `null` היכן שהמקור הראה `"--"` (מתחת לסף דיווח).
`CITY_DATA.cities` שמור **ממוין מראש יורד לפי הערך האחרון** (כמו בטבלת המקור).

**מה נוסף לדשבורד** (טאב "לפי יישוב", בין "לוח הנתונים" ל"איך לעדכן"):
- `buildCitySummary()`: 4 כרטיסי סיכום (סה"כ ארצי, יישוב מוביל, עלייה/ירידה חדה
  ביותר — מחושבים דינמית מ-`CITY_DATA`, לא hardcoded)
- `buildCityChart()`: גרף עמודות אופקי (Chart.js `indexAxis:'y'`), עם בורר
  Top-N (10/15/20/הכל) וצביעת עמודות לפי כיוון השינוי (ירוק/אדום/אפור-לנתון חסר)
- `buildCityTable()` + `sortedCitiesForTable()`: טבלה מלאה עם מיון בלחיצה על
  כותרת עמודה (שורת "סך הכל ארצי" תמיד נשארת נעוצה למעלה, לא ממוינת)
- לוגיקת מעבר הטאבים הוכללה (`querySelectorAll('.tab-panel')` גנרי לפי
  `tab-${target}`) במקום hardcoded לשני טאבים — עכשיו תומכת כל מספר טאבים

**בדיקות**: הותקן `jsdom` (טהור-JS, ללא `canvas` הנייטיבי) ב-scratchpad עם
`npm install`. ה-harness מחליף את ה-Chart.js המוטמע ב-stub class פשוט (כמו
בגישה המקורית שתוארה למעלה בפרק "איך לבדוק") ומוסיף `beforeParse` שמדביק
`getContext('2d')` על `HTMLCanvasElement.prototype` (כי גם עם ה-stub, קריאה
ל-`canvas.getContext` נכשלת תחת jsdom נטו בלי חבילת `canvas`). כל 30+ הבדיקות
עברו: מבנה נתונים, מיון, רינדור DOM (כולל תאי `—` לערכים חסרים), אינטראקציות
(מיון/Top-N/מעבר טאבים), וגם regression על 4 הגרפים/הטבלה המקוריים שנשארו
בלי שינוי. בנוסף `node --check` על כל בלוקי ה-`<script>` בקובץ הסופי.
