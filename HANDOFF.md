# דשבורד דירות לפי שלב בנייה — מסמך העברה ל-Claude Code

## עדכון אוטומטי (2026-08-10)
הפרויקט חי עכשיו גם ב-GitHub (`github.com/itailironne/housing-dashboard`, ציבורי,
מתפרסם דרך GitHub Pages ב-`itailironne.github.io/housing-dashboard`). יש routine
מתוזמן (Claude Code, ריצה יומית) שבודק אם התפרסמה הודעת למ"ס חדשה בסדרה, ואם כן
מעדכן/מאמת/דוחף לבד. פרטי מנגנון האיתור (חשוב!):

- **עמוד רשימת ההודעות (`SubjectPressReleases.aspx`) הוא SharePoint עם רינדור
  JS** — `curl` רגיל מחזיר מעטפת ריקה בלי קישורים אמיתיים. גם עמוד ה"נושא"
  הכללי (`/he/subjects/Pages/...`) וגם עמודי נחיתה של הודעות ספציפיות
  (`/he/mediarelease/Pages/{year}/...`) נבדקו ולא חשפו קישורי DocLib סטטיים.
  ניסיון לפנות ישירות ל-SharePoint Search REST API (`/_api/search/query`) גם
  נכשל (דף שגיאה).
- **הפתרון שעובד: חיפוש רגיל (WebSearch)** עם שאילתה כמו
  `"cbs.gov.il התחלות וגמר בנייה הודעה לתקשורת {שנה}"` — Google מחזיר את קישור
  ה-PDF הישיר מ-DocLib (לא רק עמוד נחיתה), ומשם אפשר לגזור את קובץ ה-xls הצמוד
  (אותה תיקייה/prefix, סיומת `t1.xls` במקום `b.pdf`).
- **מספרי ההודעות לא רציפים לפי הסדרה** — הלמ"ס ממספר את כל ההודעות שלה (כל
  הנושאים ביחד) ברצף לפי שנה, אז בין שתי הודעות רבעוניות באותה סדרה המספר יכול
  לקפוץ ב-50-150+. לדוגמה: הודעה על אוקטובר 2024-ספטמבר 2025 יצאה כ-`089/2026`
  (מרץ 2026), ואילו ההודעה על אפריל 2025-מרץ 2026 (זו שהדשבורד בנוי עליה) יצאה
  כ-`189/2026` (יוני 2026) — קפיצה של 100 מספרים תוך כ-3 חודשים. **אסור לנחש
  את המספר הבא ע"י תוספת קבועה** — חובה לגלות אותו כל פעם מחדש (חיפוש), ולהשוות
  לפי תאריך/תקופה בפועל (לא לפי המספר) כי המספור מתאפס כל שנה.
- טקסט ה-routine המלא (כולל כל הפרטים האלה) נמצא ב-Claude Code, routine בשם
  "Housing dashboard: CBS auto-update check" (trigger ID `trig_01KDH2juPuyEFQSycMpEFjAn`).
  אם צריך לשנות את הלוגיקה שלו — לעדכן גם שם, לא רק כאן.

## עדכון מנגנון האיתור + הפעלת התזמון המקומי (2026-08-12)

**מה היה שבור**: הסקריפט `scripts/update-dashboard.ps1` נכתב עבור Windows Task
Scheduler אבל **המשימה עצמה מעולם לא נרשמה** — לא היה `HousingDashboardAutoUpdate`
במערכת, לא הייתה תיקיית `logs/`, והסקריפט לא רץ אף פעם. בנוסף, גם אילו רץ,
הפרומפט אמר רק "להוריד את קבצי המקור" בלי לומר לאן — כלומר הקבצים היו יורדים
לתיקייה זמנית, משמשים לחילוץ המספרים, ונעלמים. לא היו מופיעים קבצי xls חדשים.

**מה תוקן**:
1. נרשמה משימה מתוזמנת `HousingDashboardAutoUpdate` (יומית, 09:30 ו-18:00,
   `-StartWhenAvailable` כדי להשלים ריצות שהוחמצו כשהמחשב היה כבוי). רצה תחת
   המשתמש הנוכחי ב-`LogonType Interactive` — כלומר **רק כשהמשתמש מחובר**.
2. הפרומפט (`scripts/update-prompt.txt`) עודכן: להוריד את קבצי המקור **לשורש
   הרפו**, בשמות המקוריים של הלמ"ס, לא למחוק ישנים (ארכיון), ולעשות להם `git add`
   מפורש בקומיט. גם t2/t3/t4 ולא רק t1.

**מנגנון איתור חדש ועדיף (נבדק 2026-08-12)** — במקום להתחיל מ-WebSearch:
- עמוד הנחיתה של כל הודעה נמצא בכתובת **צפויה מראש** לפי התקופה המכוסה:
  `.../he/mediarelease/Pages/{שנת פרסום}/התחלות-וגמר-בנייה-{חודש}-{שנה}-{חודש}-{שנה}.aspx`
  (התקופה היא חלון 12 חודשים מתגלגל שמתקדם ברבעון בכל הודעה).
- **הלמ"ס מחזירה HTTP 200 לכל כתובת** (soft-404), אז הסטטוס חסר ערך — **הגודל
  הוא הסימן**: עמוד של הודעה שפורסמה ≈ 37KB (נמדד 37,468 ו-37,588), עמוד של
  הודעה שטרם פורסמה או כתובת שגויה ≈ 2-3KB (2,056 ו-2,804). מעל 10,000 בייט =
  פורסם.
- זה נותן בדיקת "האם יצא?" דטרמיניסטית לגמרי, בלי חיפוש ובלי ניחוש מספרים.
- **מספר ההודעה עדיין לא מופיע ב-HTML הגולמי** של עמוד הנחיתה (נטען ב-JS דרך
  SharePoint listId/itemId — נבדק, אין טעם לנסות לפרסר אותו). לכן רק *אחרי*
  שהבדיקה אומרת "פורסם" עוברים ל-WebSearch כדי לגלות את המספר.
- **אזהרה חשובה על WebSearch**: התוצאות **לא ממוינות לפי עדכניות**. בבדיקה
  ב-2026-08-12 השאילתה הגנרית החזירה את הודעה 089/2026 (מרץ) כתוצאת ה-PDF
  הראשונה, ואילו 189/2026 (יוני, הנוכחית) **לא הופיעה בכלל**. אסור לקחת את
  ה-PDF הראשון כ"החדש ביותר" — תמיד לאמת את התקופה.
- נוסף fallback אחרון: אם הבדיקה אומרת "פורסם" אבל החיפוש לא מצא את המספר —
  סריקה חסומה של מספרים עוקבים (`{n}/04_{yy}_{n}t1.xls`) עם throttling של
  ~3 בקשות לשנייה ותקרה של 250 מספרים. **רק** במקרה הזה, לא כברירת מחדל.

**כתובות הקבצים** (אומת 200 לכל החמישה ב-2026-08-12, והגדלים תואמים בדיוק
לקבצים המקומיים): `https://www.cbs.gov.il/he/mediarelease/DocLib/{שנה}/{מספר}/04_{yy}_{מספר}{t1..t4}.xls`
ו-`...b.pdf`. `curl` עם User-Agent של דפדפן עובד; ה-WebFetch tool חסום.

**חלון הפרסום** (לפי בעל הדשבורד, 2026-08-13): הלמ"ס מפרסמת **בין ה-13 ל-25
בחודש**. לכן הבדיקה היומית מכסה את החלון במלואו, ואין צורך לצמצם אותה לתאריכים
ספציפיים. שימו לב: השדה `next_release_announced` ב-`data/last_release.json`
(2026-09-17) הוא מה שמופיע בתחתית ההודעה הקודמת — הוא **רמז בלבד**, לא הבטחה;
ייתכן פרסום מוקדם יותר, ולכן אסור לדלג על בדיקה רק כי התאריך המוכרז עוד לא הגיע.

## קריטי: יש **שני** פורמטים שונים לשם עמוד הנחיתה (נתגלה 2026-08-13)
הפרסום הוא **4 פעמים בשנה**, אבל חלון ינואר-דצמבר **לא מתפרסם** תחת שם החלון
המתגלגל — הוא יוצא כ**סיכום שנתי** בערך במרץ, עם slug אחר לגמרי:

| סוג | תבנית ה-slug | מתי | דוגמה (נמדד 2026-08-13) |
|---|---|---|---|
| חלון מתגלגל | `התחלות-וגמר-בנייה-{חודש}-{שנה}-{חודש}-{שנה}` | יוני / ספטמבר / דצמבר | `...אפריל-2025-מרץ-2026` = 37,468 ✅ |
| סיכום שנתי | `התחלות-וגמר-בנייה-סיכום-שנת-{שנה}` | ~מרץ | `...סיכום-שנת-2025` = 37,432 ✅ (=089/2026, 19.3.26) |

**ההוכחה**: ה-slug המתגלגל `ינואר-2025-דצמבר-2025` מחזיר 2,056 בייט גם בתיקיית
`Pages/2025` וגם ב-`Pages/2026` — כלומר מעולם לא פורסם בשם הזה. לעומת זאת
`סיכום-שנת-2025` מחזיר 37,432 ו-`סיכום-שנת-2024` מחזיר 37,448.

**המשמעות**: מי שבודק רק את ה-slug המתגלגל **יפספס הודעה אחת בכל שנה, כל שנה**.
הפרומפט עודכן לבדוק את **שני** הפורמטים בכל ריצה.

מחזור שנתי מלא: סיכום שנתי (~מרץ, מכסה ינואר-דצמבר הקודם) ← אפריל-מרץ (~יוני)
← יולי-יוני (~ספטמבר) ← אוקטובר-ספטמבר (~דצמבר).

## תקלה שנפתרה: הסקריפט המקומי נכשל בשקט (2026-08-13)
שתי הריצות האמיתיות הראשונות (12/8 ידנית, 13/8 מתוזמנת) נכשלו עם exit code 1
ולוג באורך 94 בייט שנעצר אחרי השורה "Invoking claude -p" — בלי שום הודעת שגיאה.
**הסיבה**: השילוב של `$ErrorActionPreference = 'Stop'` עם לכידת הפלט של פקודה
נייטיבית דרך `2>&1` לתוך משתנה. ב-Windows PowerShell 5.1 כל שורת stderr של exe
נעטפת ב-`NativeCommandError`, ועם `Stop` היא הופכת לשגיאה **מסיימת** — כלומר
הסקריפט מת לפני שהספיק לכתוב ללוג מה קרה. הסקריפט תוקן: הזרמים הנייטיביים
מנותבים לקבצים נפרדים (`logs/claude-stdout-*.txt`, `logs/claude-stderr-*.txt`),
קוד היציאה נרשם במפורש, ו-`$ErrorActionPreference` הוא `Continue`.
**לקח כללי**: אל תשלב `Stop` עם `2>&1` על exe ב-PowerShell 5.1.

## תקלה שנפתרה: הבדיקה הדטרמיניסטית תמיד אמרה "לא פורסם" (2026-08-18)
**מה היה שבור**: הפרומפט הורה לבנות את כתובת ה-URL עם הטקסט העברי הגולמי
(UTF-8) ולהעביר אותה ישירות ל-`curl`. בסביבת ה-bash/curl של המחשב הזה זה
**לא** מקודד אוטומטית תווים לא-ASCII בתוך ה-URL — כתובת עם עברית גולמית
מחזירה את דף ה"לא פורסם" (~2-2.8KB) **גם כשהדף בפועל פורסם**. אומת ב-2026-08-18:
כתובת ידועה כפורסמת (חלון אפריל 2025-מרץ 2026, 189/2026) החזירה 2,804 בייט
עם עברית גולמית, אבל 37,468 בייט (התוצאה הנכונה) לאחר קידוד ל-percent-encoding.
המשמעות: אם בודקים בדרך הנאיבית, **כל בדיקה בכל ריצה** תיראה כמו "עדיין לא
פורסם" — גם ברגע שהודעה אמיתית כבר יצאה — כלומר כשל שקט מובנה שהיה חוזר על
עצמו לנצח.
**התיקון**: לקודד את ה-slug העברי ל-percent-encoding לפני בנייה ה-URL (למשל
דרך ה-python.exe האמיתי, `urllib.parse.quote`), ולא להעביר עברית גולמית
ל-curl. הפרומפט (`scripts/update-prompt.txt`) עודכן להורות על זה במפורש,
כולל צעד ביקורת עצמית: לפני שסומכים על תוצאת "לא פורסם", לבדוק את מנגנון
הקידוד על כתובת שכן ידועה כפורסמת (זו של ה-release הנוכחי לפי last_release.json)
ולוודא שהיא חוזרת ~37KB.
**לקח כללי**: כשכתובת URL מכילה תווים לא-ASCII, אף פעם לא לסמוך על ה-shell/curl
לקודד אותה — לקודד במפורש ולוודא עם דוגמה ידועה-נכונה לפני שסומכים על תוצאת
בדיקה שלילית.

## טאב רביעי: מלאי קבלנים — **סדרה אחרת לגמרי** (2026-09-01)

הטאב "🏗️ מלאי קבלנים" לא מגיע מהודעת "התחלות וגמר בנייה" שמזינה את
כל שאר הדשבורד, אלא מהודעה **נפרדת**: "דירות בעסקאות נדל"ן".

| | התחלות וגמר בנייה | דירות בעסקאות נדל"ן |
|---|---|---|
| תדירות | רבעוני (4/שנה) | **חודשי** |
| מזין את | לוח הנתונים + לפי יישוב | מלאי קבלנים |
| הקובץ | `04_{yy}_{n}t1.xls` + PDF | `04_{yy}_{n}t2.xls` |
| דוגמה | 189/2026 (18.6.26) | 147/2026 (14.5.26) |

**למה זה חשוב**: בעל הדשבורד אמר "הנתונים יוצאים כל חודש בין 13 ל-25",
וזה נשמע סותר לסדרה הרבעונית — אבל הוא צדק: הוא דיבר על הסדרה האחרת.
אם מישהו אומר "חודשי" — כנראה מדובר בסדרה הזו, לא בהתחלות בנייה.

**מבנה לוח 2** (אומת על 04_26_147t2.xls, 8 עמודות, שורות נתונים מ-r10):
עמודה 0 = חודשי היצע, 1 = % שינוי במגמה, 2 = מגמה, 3 = מנוכה עונתיות,
4 = מקורי, 6 = חודש, 7 = שנה (**רק בשורות של ינואר** — לגרור קדימה).
הקובץ הוא xls ישן — `xlrd`, לא `openpyxl`.

**בדיקת תקינות**: לחשב מחדש את % השינוי מעמודת המגמה ולוודא התאמה
לעמודה 1 עד 0.15 נקודת אחוז. ב-147t2 כל 26 ההשוואות התאימו.

**⚠ אזהרה — חסימה מצד הלמ"ס**: סריקת מספרי הודעות בכוח (כ-90 בקשות
רצופות) **גרמה לחסימה** של המכונה ב-2026-09-01: כל בקשה הבאה החזירה
`Connection was reset` לזמן ממושך. בגלל זה לא הצלחנו להוריד את 216/2026
שכן קיים. **לגלות מספרי הודעות דרך WebSearch, לא דרך סריקה.**

## עדכון (2026-09-02): מלאי קבלנים → הודעה 252/2026, וגילוי דרך זיהוי טובה משמעותית

**מה עודכן**: הטאב "🏗️ מלאי קבלנים" עודכן להודעה **252/2026** (פורסמה 13/08/2026,
מכסה אפריל-יוני 2026). `STOCK_DATA` הוחלף כליל (כולל תיקונים רטרואקטיביים
שהלמ"ס עשתה למספר חודשים קודמים, כמו תמיד בלוח הזה) עד 06/26. `CITY_STOCK`
קיבל נקודה רביעית (06/26, כל 18 היישובים קיימים בסנאפשוט החדש, אין `null`).
בדיקת התאמת % שינוי מול עמודת המגמה עברה (הפרש מקסימלי 0.05 נ"א), והצטלבה גם
מול הפרוזה בעמוד 5 של ה-PDF (~84,280 דירות וחודשי היצע 26.0 בסוף יוני 2026 —
תואם `original`/`monthsSupply` בקובץ). הסדרה הראשית ("התחלות וגמר בנייה") **לא
זזה** — החלון המתגלגל הבא (יולי 2025-יוני 2026) עדיין לא פורסם (2,056 בייט).

**גילוי חשוב לריצות הבאות: ה-SharePoint REST API כן עובד, בניגוד למה שנכתב
למעלה** (זה שנבדק ב-2026-08-10/12 ותועד כ"נכשל" היה כנראה שאילתה שונה או
נתיב שגוי). מה שעובד בפועל:
1. הבאת דף הנחיתה של ההודעה (אחרי שבדיקת הגודל אמרה "פורסם") וחילוץ
   `pageListId`/`pageItemId` מתוך ה-HTML הגולמי (מחרוזת `"pageListId":"{...}"` /
   `"pageItemId":NNNNN` — קיימת תמיד, זה לא ה-listId/itemId שנטען ב-JS שתועד
   כלא נגיש).
2. קריאה ל-
   `https://www.cbs.gov.il/he/mediarelease/_api/web/lists(guid'{pageListId}')/items({pageItemId})/FieldValuesAsText`
   עם `-H "Accept: application/json;odata=verbose"` — **מחזיר 200 עם JSON תקין**,
   כולל השדות `CbsPubNumber` (למשל `"252/2026"`), `CbsDataPublishDate`
   (`"13/08/2026 13:00"`), `CbsPublishingFolderLevel1`/`Level2` (=שנה/מספר
   ההודעה, בדיוק מה שצריך כדי לבנות את נתיב ה-DocLib), ו-`Title`.
   **זה הופך את שלב ה-WebSearch למיותר לגמרי** ברוב המקרים — מדויק, לא תלוי
   בדירוג תוצאות חיפוש, ולא דורש סריקה שעלולה לגרום לחסימה (ראו האזהרה למעלה
   מ-2026-09-01). מומלץ להשתמש בזה כשלב ראשון אחרי שבדיקת הגודל אומרת "פורסם",
   ולפנות ל-WebSearch רק אם ה-API הזה מפסיק לעבוד.

## מה זה
דשבורד HTML אינטראקטיבי (`housing_construction_dashboard.html`) שמציג נתוני הלמ"ס על
דירות לפי שלב בנייה: בבנייה פעילה, גמר בנייה, התחלת בנייה, היתרי בנייה.
קובץ עצמאי לחלוטין — Chart.js מוטמע בפנים, אין תלות ברשת חיצונית.

## מקור הנתונים
טבלה 1 מהלמ"ס (`04_26_189t1.xls`), נתונים רבעוניים 2018 Q1 – 2026 Q1.
הנתונים מוטמעים בקובץ עצמו בתוך `const RAW_DATA = {...}` (בתחילת ה-`<script>` הראשי).
מבנה RAW_DATA:
- `labels`: מערך תוויות רבעונים ("2018 Q1", ...)
- `active_construction` + `active_pct`: בבנייה פעילה (אין לה trend/sa, רק original)
- `completed` / `begun` / `permits`: כל אחד עם `{original, trend, sa, pct}`

בנוסף, `const CITY_DATA = {...}` (מיד אחרי RAW_DATA) — פילוח לפי יישוב, ראו פרק
"הושלם: נתוני התחלות בנייה לפי יישוב" למטה למקור ולמבנה המלא.

## מבנה הקוד (בתוך ה-HTML)
- `CATEGORIES`: מערך של 4 קטגוריות עם `key/title/subtitle/color`, ול-`begun` יש גם
  `showSAOverlay: true`
- `getSeriesData(cat)`: מחזיר את המערך המלא (original/trend/sa) לפי הבחירה בתפריט
- `sliceData(arr, start, end)`: חיתוך טווח (כולל end, inclusive)
- `computeYoYGrowth(fullSeries)`: צמיחה שנה מול שנה (4 רבעונים אחורה), **מחושב על
  המערך המלא ולא על החלק החתוך**, כדי שנקודות בתחילת טווח מצומצם עדיין יראו נתון
- `buildCharts()`: בונה מחדש את כל 4 הכרטיסים/הגרפים (הורס ובונה instances, לא מעדכן)
- `updateCharts()`: קורא ל-`buildCharts()` מחדש (rebuild מלא, לא patch)
- `buildTable()`, `buildSummary()`: הטבלה המלאה וכרטיסי הסיכום למעלה
- `buildCitySummary()`, `buildCityChart()`, `buildCityTable()`,
  `sortedCitiesForTable()`, `citiesByLatestValue()`: טאב "לפי יישוב" (ראו פרק
  ייעודי למטה)
- מעבר טאבים: לולאה גנרית על `.tab-panel` לפי `panel.id === 'tab-' + target`
  (לא hardcoded למספר טאבים ספציפי — תומך הוספת טאבים נוספים בעתיד בלי שינוי
  בלוגיקת המעבר עצמה)

## פיצ'רים שנבנו (בסדר כרונולוגי)
1. דשבורד בסיסי: 4 גרפים + טבלה + כרטיסי סיכום, RTL עברית, עיצוב כהה
2. תיקון קריטי: Chart.js **מוטמע בקובץ** (לא CDN) + עטוף ב-IIFE שמסתיר
   `define/module/exports` — כדי למנוע התנגשות UMD עם AMD loaders בסביבת התצוגה
   (זו הייתה הסיבה ל-"Chart is not defined" שקרה גם אחרי הטמעה ראשונית)
3. בורר טווח זמן: כפתורים קבועים (8 רבעונים / 5 שנים / הכל) + בורר מותאם אישית
   (`customFrom`/`customTo` selects)
4. קו צמיחה על ציר Y משני (`yAxisID: 'y1'`) — עבר כמה איטרציות:
   - קודם: רגרסיה לינארית על כל הטווח (בוטל)
   - אז: רבעון מול רבעון (בוטל)
   - **גרסה נוכחית: שנה מול שנה** (רבעון X מול אותו רבעון בשנה הקודמת)
5. קו "מנוכה עונתיות" (SA) — overlay נוסף על גרף "התחלת בנייה" בלבד (`showSAOverlay`)
6. טאב נפרד "🔄 איך לעדכן נתונים" עם הוראות איך לרענן את הדשבורד כשיוצא קובץ חדש
7. טאב שלישי "🏙️ לפי יישוב" — פילוח התחלות בנייה לפי 33 יישובים (סה"כ מעל 500
   דירות ב-12 החודשים האחרונים), נתון שנתי מתגלגל מ"לוח א'" בהודעת הלמ"ס
   (ולא מקובץ ה-xls של טבלה 1). כרטיסי סיכום, גרף עמודות אופקי עם בורר Top-N,
   וטבלה מלאה עם מיון בלחיצה על כותרת
8. הטבלה המלאה הומרה ל**גרף מגמות** (small multiples): כרטיס לכל
   יישוב עם sparkline של 4 התקופות, שם, ערך אחרון ואחוז שינוי.
   **למה לא גרף קווים אחד עם 33 קווים**: 33 סדרות על ציר אחד = ספגטי,
   ואין 33 גוונים שניתן להבחין ביניהם. נקודות חשובות:
   - **לכל כרטיס קנה מידה משלו** (אחרת יישובים קטנים נראים קו שטוח) —
     לכן **אסור להשוות גובה בין כרטיסים**; זה מצוין מפורשת בכיתוב.
   - **תקופה חסרה (null) שוברת את הקו** ולא מחוברת באינטרפולציה
     (רלוונטי לאור יהודה ואלעד).
   - **חיצי ▲/▼ ליד אחוז השינוי**: בדיקת פלטה הראתה שהירוק/אדום
     (#38d39f / #ff6b81) הם במרחק CVD ΔE 5.5 בלבד תחת עוורון אדום-ירוק —
     כלומר **לא ניתן להבחנה**. אסור להסתמך על צבע לבד לכיוון.
     (הערה: גרף העמודות שמעליו עדיין צובע לפי כיוון בלבד — שווה לתקן.)
   - **הטבלה לא נמחקה** — יש מתג "גרף מגמות / טבלה", וה-tooltip מציג
     את הערכים המדויקים, כך ששום מידע לא אבד.
   - ממומש ב-**SVG inline** ולא ב-33 instances של Chart.js (משקל וביצועים).

## איך לבדוק שהכל עובד (בלי דפדפן אמיתי)
נבנתה שיטת בדיקה עם jsdom + node-canvas (ראה היסטוריית השיחה) כי:
- Chart.js לא ניתן לבדיקה ישירה תחת jsdom+node-canvas עקב חוסר תאימות (`getAttribute`,
  `setTransform` וכו') — זו מגבלת סביבת בדיקה, לא באג אמיתי
- הפתרון: הסרת ספריית Chart.js האמיתית מה-HTML לבדיקה, והזרקת מחלקת `Chart` stub
  פשוטה דרך `beforeParse` של jsdom, כדי לבדוק את **הלוגיקה שלנו** (datasets, טווחים,
  toggles) בלי להריץ רינדור קנבס אמיתי
- כל שינוי קוד לוּוה ב: `node --check` לבדיקת תחביר + סקריפט jsdom ממוקד שמוודא
  שהערכים המחושבים תואמים בדיוק לנתוני המקור (בדקנו את זה גם ידנית מול העמודות
  המקוריות בקובץ ה-xls)

## הושלם: נתוני התחלות בנייה לפי יישוב (2026-08-10)
נוסף טאב שלישי "🏙️ לפי יישוב" עם פילוח עירוני, בהמשך למשימה שהייתה פתוחה כאן.

**גישה ל-PDF**: בניגוד למה שנכתב למעלה, `curl` ישיר (עם User-Agent של דפדפן) דרך
Bash **כן הצליח** להוריד את ה-PDF מ-cbs.gov.il (200 OK, ~1MB) — למרות שה-WebFetch
tool/גלישה בצ'אט חסומים. כנראה שה-robots.txt/חסימה חלים ברמת ה-tool הספציפי ולא
ברמת הרשת של הטרמינל. אם זה חוזר לקרות בעתיד: **תמיד לנסות `curl` ישיר קודם**
לפני לבקש מהמשתמש להעלות קובץ ידנית.

**חילוץ הטבלה**: `pdftotext -layout` נתן טקסט עם שמות יישובים משובשים/חסרים
(בעיית קידוד RTL) וסדר עמודות לא אמין. הפתרון: Python (`pdfplumber`,
`page.extract_tables()`) עם ה-python.exe האמיתי (לא ה-stub של
`AppData\Local\Microsoft\WindowsApps\python.exe` שמפנה ל-Microsoft Store) —
`C:\Users\Ayelet Lironne\AppData\Local\Programs\Python\Python312\python.exe`.
ה-extraction נתן שמות יישובים הפוכים תו-אחר-תו (למשל `'יצרא לכה ךס'` במקום
`'סך הכל ארצי'`) — תופעה נורמלית של pdfplumber עם RTL, פשוט להפוך את המחרוזת.
המספרים אומתו בשתי דרכים: (1) חישוב מחדש של אחוז השינוי מתוך שני הערכים
האחרונים והשוואה לעמודת "אחוז שינוי" שבטבלה, (2) השוואה לטקסט הנרטיבי בעמוד 6
של ההודעה (שמזכיר כמה ערים ספציפיות ואחוזים). שתי הבדיקות עברו ב-100%.

**המבנה שנוסף** (`CITY_DATA`, ב-`<script>` הראשי, אחרי `RAW_DATA`): 33 יישובים
(כל אלה עם מעל 500 דירות ב-12 החודשים אפריל 2025–מרץ 2026) + שורת `total`,
כל אחד עם `values` (4 תקופות שנתיות מתגלגלות, 2022-23 עד 2025-26) ו-`pctChange`.
`values`/`pctChange` הם `null` היכן שהמקור הראה `"--"` (מתחת לסף דיווח).
`CITY_DATA.cities` שמור **ממוין מראש יורד לפי הערך האחרון** (כמו בטבלת המקור).

**מה נוסף לדשבורד** (טאב "לפי יישוב", בין "לוח הנתונים" ל"איך לעדכן"):
- `buildCitySummary()`: 4 כרטיסי סיכום (סה"כ ארצי, יישוב מוביל, עלייה/ירידה חדה
  ביותר — מחושבים דינמית מ-`CITY_DATA`, לא hardcoded)
- `buildCityChart()`: גרף עמודות אופקי (Chart.js `indexAxis:'y'`), עם בורר
  Top-N (10/15/20/הכל) וצביעת עמודות לפי כיוון השינוי (ירוק/אדום/אפור-לנתון חסר)
- `buildCityTable()` + `sortedCitiesForTable()`: טבלה מלאה עם מיון בלחיצה על
  כותרת עמודה (שורת "סך הכל ארצי" תמיד נשארת נעוצה למעלה, לא ממוינת)
- לוגיקת מעבר הטאבים הוכללה (`querySelectorAll('.tab-panel')` גנרי לפי
  `tab-${target}`) במקום hardcoded לשני טאבים — עכשיו תומכת כל מספר טאבים

**בדיקות**: הותקן `jsdom` (טהור-JS, ללא `canvas` הנייטיבי) ב-scratchpad עם
`npm install`. ה-harness מחליף את ה-Chart.js המוטמע ב-stub class פשוט (כמו
בגישה המקורית שתוארה למעלה בפרק "איך לבדוק") ומוסיף `beforeParse` שמדביק
`getContext('2d')` על `HTMLCanvasElement.prototype` (כי גם עם ה-stub, קריאה
ל-`canvas.getContext` נכשלת תחת jsdom נטו בלי חבילת `canvas`). כל 30+ הבדיקות
עברו: מבנה נתונים, מיון, רינדור DOM (כולל תאי `—` לערכים חסרים), אינטראקציות
(מיון/Top-N/מעבר טאבים), וגם regression על 4 הגרפים/הטבלה המקוריים שנשארו
בלי שינוי. בנוסף `node --check` על כל בלוקי ה-`<script>` בקובץ הסופי.
