You're maintaining a public housing-statistics dashboard for its owner, running locally on their own PC (this repo is already cloned at the current working directory, and `git push` works without prompting because a GitHub credential is already cached here — you don't need to set up auth). Repo root has `housing_construction_dashboard.html` (the live dashboard: a self-contained single-file HTML page with embedded Chart.js and two embedded JS data objects, `RAW_DATA` and `CITY_DATA`), `HANDOFF.md` (full technical documentation of data structures, extraction methodology, and known gotchas — READ THIS FIRST, it's essential context), `data/last_release.json` (state file recording the last CBS press release this repo's data reflects), `index.html` (a redirect stub to the dashboard file — leave it alone), and `README.md`. This repo is served live via GitHub Pages at the repo root, so pushing to `main` is what makes an update go live.

This run is fully unattended (no one will see prompts or answer questions) — never wait for confirmation, just follow the steps below to their conclusion.

## Your task, each run

1. Read HANDOFF.md fully before doing anything else.
2. Read data/last_release.json to see which CBS press release (e.g. "189/2026") and which published date/period_covered this repo's data currently reflects.
3. Find the newest release in this series ("התחלות וגמר בנייה" / Construction Begun and Completed) and determine if it's actually newer than what's recorded:
   - The CBS listing/search page for this subject (https://www.cbs.gov.il/he/cbsNewBrand/Pages/Search/SubjectPressReleases.aspx?CbsSubject=...) is a JavaScript-rendered SharePoint search page — plain `curl` returns an empty shell with no real release links. Don't rely on fetching it directly; this was verified to fail.
   - **First, run the deterministic "has it dropped yet?" probe — do NOT lead with WebSearch.** Each release covers a rolling 12-month window that advances one quarter, and its landing page URL is fully predictable from that window:
     `https://www.cbs.gov.il/he/mediarelease/Pages/{publication_year}/התחלות-וגמר-בנייה-{from_month}-{from_year}-{to_month}-{to_year}.aspx`
     (URL-encoded; Hebrew month names, hyphens between every token). So if last_release.json says `period_covered` is "אפריל 2025 - מרץ 2026", the next window is "יולי 2025 - יוני 2026", i.e. `.../Pages/2026/התחלות-וגמר-בנייה-יולי-2025-יוני-2026.aspx`. Near a year boundary the publication_year folder is the calendar year the release comes out in, so try both.
     CBS **soft-404s**: every URL returns HTTP 200, so status code tells you nothing — **use response SIZE**. Measured on 2026-08-12: a published release's landing page is ~37 KB (37,468 and 37,588 bytes for two real ones), while an unpublished/nonexistent one is ~2-3 KB (2,056 and 2,804 bytes). Treat >10,000 bytes as "published", <5,000 bytes as "not yet". `curl -s -A "<browser UA>" -o /dev/null -w "%{size_download}" -L "<url>"`.
     If the probe says "not yet", that is a clean step-4 no-op — stop there, no search needed, no failure report.
   - **Only if the probe says the new release IS published**, find its release number, which the landing page does not contain in its raw HTML (it's fetched client-side via SharePoint listId/itemId, verified 2026-08-12 — don't bother parsing the page for it). Use WebSearch, ideally querying the new period by name (e.g. "cbs.gov.il התחלות וגמר בנייה יולי 2025 יוני 2026"), falling back to "cbs.gov.il התחלות וגמר בנייה הודעה לתקשורת {current year}". **Beware: search ranking is NOT recency-ordered** — a 2026-08-12 test of the generic query returned release 089/2026 (March) as the top direct-PDF hit while the then-current 189/2026 (June) did not appear at all. Never take the first PDF hit as "newest"; always verify the candidate's period against what you're looking for. Search results give the DIRECT PDF URL itself, of the form `https://www.cbs.gov.il/he/mediarelease/DocLib/{year}/{release_number}/{subject_code}_{yy}_{release_number}b.pdf` — subject_code is "04" for this series and {yy} is the 2-digit publication year, but {release_number} is NOT predictable or sequential-by-quarter: CBS numbers press releases sequentially across ALL its publications that year (every subject combined), so it can jump by 50-150+ between consecutive quarterly releases of this one series. Never guess it by incrementing from the last known number in data/last_release.json — always discover it via search.
   - Once you have a candidate PDF URL, derive the sibling quarterly Excel file URLs by keeping the same DocLib folder/{code}_{yy}_{release_number} prefix and swapping the suffix to `t1.xls`, `t2.xls`, `t3.xls`, `t4.xls` (e.g. `..._189b.pdf` siblings are `..._189t1.xls` … `..._189t4.xls`). Table 1 is the one the dashboard's RAW_DATA is built from and is REQUIRED; tables 2-4 are archived alongside it for the owner's reference — if one of them 404s, skip just that file and carry on, but a missing t1 or missing PDF is a step-6 failure. Verify each URL actually resolves — `curl -A "Mozilla/5.0 (Windows NT 10.0; Win64; x64) AppleWebKit/537.36 (KHTML, like Gecko) Chrome/120.0.0.0 Safari/537.36" -o /dev/null -w "%{http_code}" <url>` should return 200 — before treating it as real; don't assume the pattern holds without checking (plain curl with a browser User-Agent reliably works for these static DocLib files from this machine, even though cbs.gov.il blocks some automated fetch tools via robots.txt).
   - Confirm this candidate is actually newer: open the PDF and check its stated publish date / covered period (near the top, e.g. "18 ביוני, 2026" and "אפריל 2025-מרץ 2026") against last_release.json's `published`/`period_covered`. Release NUMBERS reset each year, so don't compare them directly across a year boundary — compare actual dates/periods.
   - Last-resort fallback, allowed ONLY when the size-probe above already confirmed the release is published but WebSearch failed to surface its number: scan for it. CBS numbers releases sequentially by date across all subjects (~33/month observed: 089 on 2026-03-19 → 189 on 2026-06-18), so probe `https://www.cbs.gov.il/he/mediarelease/DocLib/{year}/{n}/04_{yy}_{n}t1.xls` for n from last_known+1 upward, zero-padded to 3 digits, **throttled to ~3 requests/second with `sleep 0.3` between them**, stopping at the first that returns a real xls (check `%{size_download}` > 50000, not just the status code) and capping the scan at 250 numbers. Do not run this scan when the size-probe says the release isn't out — there would be nothing to find and it would just hammer their server.
   - If the size-probe says published but BOTH search and the bounded scan come up empty, or the discovered candidate URLs don't resolve, that is a step-6 failure (report exactly what you tried and found) — not a reason to guess or fall back to a stale pattern.
4. If there is NO newer release than recorded: do nothing further, don't commit, end the session.
5. If there IS a newer release:
   a. Download the source files INTO THE REPO ROOT, keeping CBS's own filenames exactly as they appear in the URL (e.g. `04_26_189t1.xls`, `04_26_189b.pdf`) — do not rename them, do not put them in a temp directory, do not delete them at the end of the run. These archived source files are a deliverable in their own right: the owner wants each new release's raw files to show up in the repo folder. Download: every Excel table that resolves (`t1.xls` through `t4.xls`; t1 = dwellings by stage of construction, the one RAW_DATA comes from) and the PDF press release containing "Luach A"/"לוח א" (dwellings whose construction began, by selected localities). Leave the PREVIOUS release's source files in place too — they are an archive, so never delete or overwrite older ones (filenames differ by release number, so they won't collide).
   b. Extract the data following exactly the structures documented in HANDOFF.md: RAW_DATA (labels + active_construction/active_pct + completed/begun/permits each with {original, trend, sa, pct}) and CITY_DATA (years + total{name,values,pctChange} + cities[{name,values,pctChange}] for every locality above the threshold stated in that release's table footnote). Use a real Python PDF-table library such as pdfplumber (`pip install pdfplumber` if needed) for Luach A — do NOT rely on `pdftotext -layout`, it garbles Hebrew RTL text/column alignment. pdfplumber's `extract_tables()` returns city names character-reversed (raw glyph order) — reverse each string to get correct Hebrew. On this machine, the real Python is at `C:\Users\Ayelet Lironne\AppData\Local\Programs\Python\Python312\python.exe` (not the `python`/`python3` on PATH, which are Microsoft Store stub aliases that don't work).
   c. VALIDATE before touching the dashboard: recompute each pctChange field from the raw values and confirm it matches the source's stated percentage within ~0.15 percentage points; cross-check several figures against the narrative/summary prose elsewhere in the same PDF (it restates specific numbers). If anything doesn't reconcile, or the table structure differs from what HANDOFF.md describes, STOP and go to step 6 — do not guess or partially update.
   d. Update `housing_construction_dashboard.html` in place: extend/revise RAW_DATA and CITY_DATA (keep the exact JS object shape/key names other logic depends on — check for revisions to already-published provisional quarters too), update header/labels stating the quarter range and publish date, update footer/source-note dates. Don't change styling, structure, or unrelated logic. Keep it a single self-contained file (embedded Chart.js, no external runtime network dependency — fetching sources during this maintenance run is fine).
   e. Update `data/last_release.json` (release number, published date, period covered, source file names/URLs, next announced release date from the new PDF's footer, checked_at = today).
   f. Append a short dated entry to HANDOFF.md under a "## עדכונים אוטומטיים" section (create it if missing) describing what changed, in Hebrew, matching that doc's existing style.
   g. If `STATUS.md` exists at repo root from a previous failed run, delete it now and commit that deletion too.
   h. Commit all changed files — including the newly downloaded `.xls`/`.pdf` source files from step 5a, which are new untracked files and need an explicit `git add` (e.g. "Auto-update: CBS release {number} ({date})") — and push to `main`.
6. Failure path (unreachable/undiscoverable source, unparseable table, or validation mismatch in 5c): do NOT modify housing_construction_dashboard.html or data/last_release.json. Create/update `STATUS.md` at repo root describing exactly what was tried and what failed (search queries used, candidate URLs tried, any errors) so the owner can investigate manually. Commit and push just that file ("Auto-update check failed: see STATUS.md").

## Ground rules
- Never invent or estimate data — if a number can't be verified against the source, leave it out and fail loudly via STATUS.md instead.
- Keep the dashboard a single self-contained HTML file.
- Preserve the existing Hebrew RTL layout and dark styling (CSS variables in :root) — don't restyle.
